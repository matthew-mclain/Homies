<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Homies"
        tools:targetApi="31">
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".ui.messages.MessagesActivity"
            android:exported="true"
            android:label="Messages">
        </activity>

        <activity
            android:name=".ui.grocery_list.GroceryListActivity"
            android:exported="true"
            android:label="GroceryList">
        </activity>

        <activity
            android:name=".ui.laundry.LaundryActivity"
            android:exported="true"
            android:label="Laundry">
        </activity>

        <activity
            android:name=".ui.calendar.CalendarActivity"
            android:exported="true"
            android:label="Calendar">
        </activity>

        <activity
            android:name=".ui.location.LocationActivity"
            android:exported="true"
            android:label="Location">
        </activity>

    </application>

</manifest>